import 'package:flutter/material.dart';
import 'package:app/theme.dart';

class SignUpPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    setNotificationBar(THEME.dark);

    return Scaffold(
      body: Center(
        child: Text('Yollooooo')
      )
    );
  }
}
